import PropTypes from 'prop-types';

const propTypes = {};

const defaultProps = {};

const XX = props => {
  return <div />;
};

XX.propTypes = propTypes;
XX.defaultProps = defaultProps;

export default XX;
