import styled from 'styled-components';

const BtnProfile = styled.button`
  display: flex;
  flex-direction: row;
  align-items: center;
  border: none;
  font-size: 16px;
  cursor: pointer;
  :focus {
    outline: none;
  }
`;

export default BtnProfile;
